#
# Copy this file to `.env` for local development or provision the same keys through
# your deployment secret manager. Never commit real credentials, API keys, tokens,
# private keys, or production database URLs.
#
NODE_ENV=development
PORT=8080
APP_BASE_URL=http://localhost:8080
HOST=127.0.0.1
TRUST_PROXY=0
FORCE_HTTPS=false
HSTS_MAX_AGE_SECONDS=31536000
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_MAX_REQUESTS=240
API_RATE_LIMIT_BLOCK_MS=60000
SLOW_REQUEST_THRESHOLD_MS=2500

# PostgreSQL
# In production, keep PostgreSQL off the public internet. Use a unix socket, private IP,
# or a host protected by private networking / firewall allowlists only.
DATABASE_URL=postgresql://<db-user>:<db-password>@127.0.0.1:5432/meetscheduling
DATABASE_SSL_ENABLED=false
DATABASE_SSL_REJECT_UNAUTHORIZED=true
DATABASE_PRIVATE_NETWORK_REQUIRED=false
DATABASE_PRIVATE_NETWORK_ASSERTED=false

# Auth and signing
JWT_SECRET=<generate-a-32-byte-random-secret>
PUBLIC_BOOKING_SIGNING_SECRET=<generate-a-32-byte-random-secret>
AUTH_COOKIE_SAME_SITE=lax
AUTH_COOKIE_SECURE=false

# Scheduling defaults
HOST_DEFAULT_TIMEZONE=UTC
SLOT_INTERVAL_MINUTES=15

# SMTP Email (optional for confirmations)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
SUPPORT_EMAIL=support@meetscheduling.com
AUTH_WELCOME_EMAIL_ENABLED=true
AUTH_LOGIN_EMAIL_ENABLED=true
PASSWORD_RESET_TOKEN_TTL_MINUTES=60

# ─────────────────────────────────────────────────────────────────────────────
# Google Calendar OAuth — Required for Google Calendar + Google Meet integration
# ─────────────────────────────────────────────────────────────────────────────
# Setup steps:
#   1. Go to https://console.cloud.google.com → APIs & Services → Library
#      Enable: "Google Calendar API" and "People API"
#
#   2. Go to APIs & Services → OAuth consent screen
#      - User type: External
#      - Add scopes:
#          https://www.googleapis.com/auth/calendar.events
#          https://www.googleapis.com/auth/userinfo.email
#
#   3. Go to APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
#      - Application type: Web application
#      - Authorized redirect URIs — add EXACTLY (no trailing slash):
#          https://yourdomain.com/api/integrations/google-calendar/callback
#          (also add http://localhost:8080/api/integrations/google-calendar/callback for dev)
#
#   4. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://yourdomain.com/api/integrations/google-calendar/callback
GOOGLE_OAUTH_DEBUG=false

# ─────────────────────────────────────────────────────────────────────────────
# CRITICAL: Token encryption secret — must be set in production
# Without this, Google OAuth tokens cannot be stored/retrieved correctly and
# Google Calendar will always appear as "disconnected" after connecting.
#
# Generate a secure value with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# WARNING: Never change this after users have connected Google Calendar.
#          Changing it will invalidate all stored tokens and force re-connection.
# ─────────────────────────────────────────────────────────────────────────────
INTEGRATION_TOKEN_SECRET=<generate-a-32-byte-random-secret>

# PayPal Billing
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=
PAYPAL_SECRET=
PAYPAL_WEBHOOK_ID=
PAYPAL_PRODUCT_ID=
PAYPAL_BASIC_PLAN_ID=
PAYPAL_POPULAR_PLAN_ID=
PAYPAL_PRO_PLAN_ID=
