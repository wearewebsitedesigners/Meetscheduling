NODE_ENV=development
PORT=8080
APP_BASE_URL=http://localhost:8080

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/meetscheduling

# Auth and signing
JWT_SECRET=replace-with-strong-random-secret
PUBLIC_BOOKING_SIGNING_SECRET=replace-with-strong-random-secret

# Scheduling defaults
HOST_DEFAULT_TIMEZONE=UTC
SLOT_INTERVAL_MINUTES=15

# SMTP Email (optional for confirmations)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Google Calendar (optional integration scaffolding)
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=

